# Railway Railpack configuration for pnpm
# Railway will use this to detect pnpm instead of npm

[build]
install = "npm install -g pnpm && pnpm install --frozen-lockfile"
build = "pnpm prisma generate && pnpm prisma db push"

[start]
cmd = "pnpm dev"

